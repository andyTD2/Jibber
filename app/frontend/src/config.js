/*
    window.JIBBER_CONFIG is a JSON object that will be injected into a script inside index.html.
    This process happens on the server, prior to sending index.html
*/

const CONFIG = window.JIBBER_CONFIG;

export default CONFIG;